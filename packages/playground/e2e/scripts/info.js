console.log(`---------------------------------------------------------------------------------
✅ E2E Tests For Studio ✅
    
In order to run the e2e tests, you need to build the entire monorepo first.

    👉 pnpm build # at root

Then, you can run the e2e tests:

    👉 pnpm test:e2e # in packages/playground

The tests will run in Playwright.
---------------------------------------------------------------------------------
    `);
